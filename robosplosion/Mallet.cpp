#include "mallet.h"

/*Mallet::Mallet(Servo * s) {
  malletServo = s;
  malletServo->attach(MALLET_PIN);
  malletServo->write(MALLET_RETRACT_POS);
}

void Mallet::swing(void) {
  malletServo->write(MALLET_EXTEND_POS);
  delay(MALLET_SNAP_DURATION);
  malletServo->write(MALLET_SNAP_POS);
}

void Mallet::retract(void) {
  malletServo->write(MALLET_RETRACT_POS);
  delay(MALLET_RETRACT_DURATION);
}*/

